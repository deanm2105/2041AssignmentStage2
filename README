INSTRUCTIONS:

This is the dump from git archive, I figure you can just use git to see verions etc. from here.

The git log is probably better than my diary as well. For the next stage I think I'm just going to
export the log as my diary... (I was using git for the first time on this assignment)
